{
  "name": "waffer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node scripts/check-static.mjs",
    "test": "node --test tests/*.test.mjs",
    "ci": "npm run check && npm test"
  }
}
